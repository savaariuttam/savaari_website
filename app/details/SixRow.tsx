import Button from '../components/UI/Button';
import React from 'react';

const SixRow = () => {
    return (
        <div className="sm:w-full md:w-full lg:w-full mt-4 w-full">
            <Button href=" " name="Search Flights" className='btn-block' />
        </div>

    );
};

export default SixRow;